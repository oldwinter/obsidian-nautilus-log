{
  "schema_version": 1,
  "passing": "all-record-types",
  "failing": [
    { "id": "malformed-record", "expected": "malformed JSON" },
    { "id": "missing-record", "expected": "must exhaustively list" },
    { "id": "duplicate-evidence", "expected": "duplicate evidence IDs" },
    { "id": "stale-candidate", "expected": "stale candidate SHA" },
    { "id": "stale-package", "expected": "stale package SHA-256" },
    { "id": "corrupted-artifact", "expected": "SHA-256 mismatch" },
    { "id": "corrupted-package", "expected": "SHA-256 mismatch" },
    { "id": "corrupted-owner", "expected": "only in evidence arrays" },
    { "id": "missing-evidence", "expected": "must be a non-empty array" },
    { "id": "duplicate-test", "expected": "must not contain duplicates" },
    { "id": "path-traversal", "expected": "without traversal" },
    { "id": "symlink-artifact", "expected": "symbolic links are forbidden" },
    { "id": "skipped", "expected": "must equal false" },
    { "id": "quarantined", "expected": "must equal false" },
    { "id": "retry", "expected": "must equal 0" },
    { "id": "expected-failure", "expected": "must equal false" },
    { "id": "failed-result", "expected": "must equal PASS" },
    { "id": "symbolic-tool-version", "expected": "must record an exact value" },
    { "id": "min-supported-token", "expected": "must record an exact value" },
    { "id": "current-stable-token", "expected": "must record an exact value" },
    { "id": "incomplete-host-profile", "expected": "requires exact electron version" },
    { "id": "invalid-visual-dpr", "expected": "requires DPR 1" },
    { "id": "invalid-a11y-zoom", "expected": "requires 200 percent zoom" },
    { "id": "invalid-timestamp", "expected": "valid calendar date" },
    { "id": "stale-requirements-object", "expected": "stale candidate object" },
    { "id": "invalid-object-id-length", "expected": "full lowercase Git object ID" },
    { "id": "alternate-requirements-path", "expected": "expected exact candidate path" },
    { "id": "corrupted-index", "expected": "SHA-256 mismatch" }
  ]
}
